<!DOCTYPE html>
<html lang="en-US">
<head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/public/css/site.css">
</head>
<body>
<header>
    <article class="new-post">
  {{#if isloggedin}}
  <h1> Hi {{ user.username }}</h1>
<form action="/users/logout" method="GET" class="new-post-form">
        <button type="submit"> Logout </button>
</form>

<a href="/users/userdetails">My profile</a>
<a href="#">Add an item for Bid</a>
  

  {{else}}

<h1> Hi Stranger , Why don't you login or Sign up?</h1>
<div class="btn-group">
  <form action="/users" method="GET" class="new-post-form">
        <button type="submit">Sign Up as a new user?</button>
  </form>

  <form action="/users/userlogin" method="GET" class="new-post-form">
      <button type="submit">Login</button>
  </form>
</div>

  {{!-- <form action="/users/userlogin" method="POST" class="new-post-form">
    <label>
      Username:
      <input type="text" name="username" placeholder="" value="{{userinfo.username}}" class="full-width-input"/>
    </label>
      Password:
      <input type="password" name="password" placeholder="" value="{{userinfo.password}}" class="full-width-input"/>
    </label>

 
    <button type="submit">Login</button>
    <button type="reset">Clear Form</button>
    </form>
  {{#if hasErrors}}
  <ul class="error-list">
    {{#each errors}}
    <li>
      {{this}}
    </li>
    {{/each}}
  </ul>
  {{/if}} --}}
{{/if}}
</article>
<div class="dropdown">
  <button class="dropbtn">Categories</button>
  <div class="dropdown-content">
    <a href="#">Electronics</a>
    <a href="#">Automobile</a>
    <a href="#">Furniture</a>
  </div>
</div>
</header>

    {{{ body }}}
</body>
</html>